
[tool.poetry]
name = "app-mecoflix"
version = "0.1.0"
description = "A project for managing movie collections"
authors = ["Javier MR <javiermr@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
pytest = "^6.2.4"

[build-system]
requires = ["poetry-core>=1.0.0", "shell"]
build-backend = "poetry.core.masonry.api"